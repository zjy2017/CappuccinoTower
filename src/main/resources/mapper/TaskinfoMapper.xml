<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dao.TaskinfoMapper" >

	<!-- pojo类属性和对应表的结果集映射关系 -->
	<resultMap id="BaseResultMap" type="com.pojo.Taskinfo">
		<id column="taskinfo_id" property="taskinfoId" jdbcType="INTEGER" />
		<result column="taskinfo_name" property="taskinfoName" jdbcType="VARCHAR" />
		<result column="takinfo_describe" property="takinfoDescribe" jdbcType="CLOB" />
		<result column="task_id" property="taskId" jdbcType="INTEGER" />
		<result column="taskinfo_user" property="taskinfoUser" jdbcType="INTEGER" />
		<result column="p_id" property="pId" jdbcType="INTEGER" />
	</resultMap>

	<!-- 条件从句 -->
	<sql id="Example_Where_Clause">
		<where>
			<!-- 读取criteria列表集合中的元素 -->
			<foreach collection="oredCriteria" item="criteria" separator="or">
				<!-- 判断criteria是否有条件 -->
				<if test="criteria.valid">
					<!-- 设置前后缀包裹，以and分隔 -->
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<!-- 遍历每一个criteria中的List<Criterion>自定义条件列表集合 -->
						<foreach collection="criteria.criteria" item="criterion">
							<!-- 有选择的插入，相当于switch -->
							<choose>
								<!-- 无数值数据，用于field is null和field is not null -->
								<when test="criterion.noValue">
									and ${criterion.condition}
								</when>
								<!-- 单一数据 -->
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
								</when>
								<!-- 两个数据，用于between和not between语句 -->
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
								</when>
								<!-- 用于in和not in -->
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem"
										open="(" close=")" separator=",">
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>

	<!-- 条件从句 -->
	<sql id="Update_By_Example_Where_Clause">
		<where>
			<!-- 读取criteria列表集合中的元素 -->
			<foreach collection="example.oredCriteria" item="criteria" separator="or">
				<!-- 判断criteria是否有条件 -->
				<if test="criteria.valid">
					<!-- 设置前后缀包裹，以and分隔 -->
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<!-- 遍历每一个criteria中的List<Criterion>自定义条件列表集合 -->
						<foreach collection="criteria.criteria" item="criterion">
							<!-- 有选择的插入，相当于switch -->
							<choose>
								<!-- 无数值数据，用于field is null和field is not null -->
								<when test="criterion.noValue">
									and ${criterion.condition}
								</when>
								<!-- 单一数据 -->
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
								</when>
								<!-- 两个数据，用于between和not between语句 -->
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
								</when>
								<!-- 用于in和not in -->
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem"
										open="(" close=")" separator=",">
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>

	<sql id="Base_Column_List">
		taskinfo_id,
		taskinfo_name,
		takinfo_describe,
		task_id,
		taskinfo_user,
		p_id
	</sql>
	<!-- 统计符合自定义条件的数据个数 -->
	<select id="countByExample" parameterType="com.pojo.TaskinfoExample"
		resultType="java.lang.Integer">
		select count(*) from taskinfo
		<!-- _parameter代表的时传入参数 -->
		<!-- 自定义条件 -->
		<if test="_parameter != null">
			<include refid="Example_Where_Clause" />
		</if>
	</select>

	<!-- 自定义条件删除数据 -->
	<delete id="deleteByExample" parameterType="com.pojo.TaskinfoExample">
		delete from taskinfo
		<!-- _parameter代表的时传入参数 -->
		<!-- 自定义条件 -->
		<if test="_parameter != null">
			<include refid="Example_Where_Clause" />
		</if>
	</delete>

	<!-- 根据主键删除单条数据 -->
	<delete id="deleteByPrimaryKey">
		delete from taskinfo
		<!-- 传入主键作为条件 -->
		where
		taskinfo_id = #{taskinfoId, jdbcType=INTEGER} 
	</delete>

	<!-- 插入单条数据 -->
	<insert id="insert" parameterType="com.pojo.Taskinfo">
		insert into taskinfo (
			taskinfo_id,
			taskinfo_name,
			takinfo_describe,
			task_id,
			taskinfo_user,
			p_id
		)
		values (
			#{taskinfoId, jdbcType=INTEGER},
			#{taskinfoName, jdbcType=VARCHAR},
			#{takinfoDescribe, jdbcType=CLOB},
			#{taskId, jdbcType=INTEGER},
			#{taskinfoUser, jdbcType=INTEGER},
			#{pId, jdbcType=INTEGER}
		)
	</insert>

	<!-- 有选择的插入单条数据 -->
	<insert id="insertSelective" parameterType="com.pojo.Taskinfo">
		insert into taskinfo
		<!-- 前缀为"("、后缀为")"、去掉最后一个"," -->
		<!-- 插入属性 -->
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="taskinfoId != null">
				taskinfoId,
			</if>
			<if test="taskinfoName != null">
				taskinfoName,
			</if>
			<if test="takinfoDescribe != null">
				takinfoDescribe,
			</if>
			<if test="taskId != null">
				taskId,
			</if>
			<if test="taskinfoUser != null">
				taskinfoUser,
			</if>
			<if test="pId != null">
				pId,
			</if>
		</trim>
		<!-- 插入内容 -->
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="taskinfoId != null">
				#{taskinfoId, jdbcType=INTEGER},
			</if>
			<if test="taskinfoName != null">
				#{taskinfoName, jdbcType=VARCHAR},
			</if>
			<if test="takinfoDescribe != null">
				#{takinfoDescribe, jdbcType=CLOB},
			</if>
			<if test="taskId != null">
				#{taskId, jdbcType=INTEGER},
			</if>
			<if test="taskinfoUser != null">
				#{taskinfoUser, jdbcType=INTEGER},
			</if>
			<if test="pId != null">
				#{pId, jdbcType=INTEGER},
			</if>
		</trim>
	</insert>

	<!-- 查询符合自定义条件的数据 -->
	<select id="selectByExample" resultMap="BaseResultMap"
		parameterType="com.pojo.TaskinfoExample">
		select
		<!-- 判断是否使用distinct查询 -->
		<if test="distinct">
			distinct
		</if>
		<!-- 查询字段 -->
		<include refid="Base_Column_List" />
		from taskinfo
		<!-- 自定义条件 -->
		<if test="_parameter != null">
			<include refid="Example_Where_Clause" />
		</if>
		<!-- 判断是否指定排序从句 -->
		<if test="orderByClause != null">
			order by ${orderByClause}
		</if>
	</select>

	<!-- 根据主键查询单条数据 -->
	<select id="selectByPrimaryKey" resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from taskinfo
		<!-- #表示占位符、$表示拼接 -->
		where
		taskinfo_id = #{taskinfoId, jdbcType=INTEGER} 
	</select>

	<!-- 自定义条件的有选择性的修改某些属性，可修改主键 -->
	<update id="updateByExampleSelective">
		update taskinfo
		<!-- 修改属性，可修改主键 -->
		<set>
			<if test="record.taskinfoId != null">
				taskinfoId = #{record.taskinfoId, jdbcType=INTEGER},
			</if>
			<if test="record.taskinfoName != null">
				taskinfoName = #{record.taskinfoName, jdbcType=VARCHAR},
			</if>
			<if test="record.takinfoDescribe != null">
				takinfoDescribe = #{record.takinfoDescribe, jdbcType=CLOB},
			</if>
			<if test="record.taskId != null">
				taskId = #{record.taskId, jdbcType=INTEGER},
			</if>
			<if test="record.taskinfoUser != null">
				taskinfoUser = #{record.taskinfoUser, jdbcType=INTEGER},
			</if>
			<if test="record.pId != null">
				pId = #{record.pId, jdbcType=INTEGER},
			</if>
		</set>
		<!-- 自定义条件 -->
		<if test="_parameter != null">
			<include refid="Update_By_Example_Where_Clause" />
		</if>
	</update>

	<!-- 自定义条件修改所有属性，可修改主键 -->
	<update id="updateByExample">
		update taskinfo
		<!-- 修改属性，可修改主键 -->
		set
		taskinfoId = #{taskinfoId, jdbcType=INTEGER},
		taskinfoName = #{taskinfoName, jdbcType=VARCHAR},
		takinfoDescribe = #{takinfoDescribe, jdbcType=CLOB},
		taskId = #{taskId, jdbcType=INTEGER},
		taskinfoUser = #{taskinfoUser, jdbcType=INTEGER},
		pId = #{pId, jdbcType=INTEGER}
		<!-- 自定义条件 -->
		<if test="_parameter != null">
			<include refid="Update_By_Example_Where_Clause" />
		</if>
	</update>

	<!-- 通过主键有选择性的修改某该属性，不可以修改主键 -->
	<update id="updateByPrimaryKeySelective" parameterType="com.pojo.Taskinfo">
		update taskinfo
		<!-- 修改属性，不可以修改主键 -->
		<set>
			<if test="taskinfoName != null">
				taskinfoName = #{taskinfoName, jdbcType=VARCHAR},
			</if>
			<if test="takinfoDescribe != null">
				takinfoDescribe = #{takinfoDescribe, jdbcType=CLOB},
			</if>
			<if test="taskId != null">
				taskId = #{taskId, jdbcType=INTEGER},
			</if>
			<if test="taskinfoUser != null">
				taskinfoUser = #{taskinfoUser, jdbcType=INTEGER},
			</if>
			<if test="pId != null">
				pId = #{pId, jdbcType=INTEGER},
			</if>
		</set>
		<!-- 主键作为条件 -->
		where
		taskinfo_id = #{taskinfoId, jdbcType=INTEGER} 
	</update>

	<!-- 通过主键修改属性，不可以修改主键 -->
	<update id="updateByPrimaryKey" parameterType="com.pojo.Taskinfo">
		update taskinfo
		<!-- 修改属性，不可以修改主键 -->
		set
		taskinfoName = #{taskinfoName, jdbcType=VARCHAR},
		takinfoDescribe = #{takinfoDescribe, jdbcType=CLOB},
		taskId = #{taskId, jdbcType=INTEGER},
		taskinfoUser = #{taskinfoUser, jdbcType=INTEGER},
		pId = #{pId, jdbcType=INTEGER}
		<!-- 主键作为条件 -->
		where
		taskinfo_id = #{taskinfoId, jdbcType=INTEGER} 
	</update>
</mapper>